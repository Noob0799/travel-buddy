import React, { Component, Fragment } from 'react'

export default class Cities extends Component {
    render() {
        return (
            <Fragment>
                Cities
            </Fragment>
        )
    }
}
